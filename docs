# autogenerate migration
alembic -c alembic.ini revision --autogenerate -m ""

# apply migrations to db via host
alembic -c fastapi/alembic.ini upgrade head

# apply migrations to DB via Docker
docker compose --profile migration run --rm migrate

# verify migrations
# docker exec -it <db_container> psql -U <db_user> -d <db_name> -c '\dt'
docker exec -it platform-db-1 psql -U platform_dev -d platform_dev_db -c '\dt'

# swagger docs
http://localhost:8000/docs